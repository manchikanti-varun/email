node_modules/
data/

# Generated frontend build output (built by `npm run build` from frontend/).
# Both Vercel and Railway build this during deploy; the source lives in frontend/.
# Anchored with a leading slash so it only ignores the ROOT public/, not
# frontend/public/ (which holds committed static assets: favicon, manifest, etc.).
/public/
# The runtime config is generated by scripts/build-frontend.mjs on each build.
/frontend/public/config.js
*.db
*.db-journal
*.db-wal
*.db-shm
uploads/
.env
.env.production
_*.txt
_*.csv
.DS_Store
*.md
!README.md
docs/
