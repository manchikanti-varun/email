// Settings page (Phase 1B). Surfaces the real account setting that exists
// today: API key management. Reuses the api feature container unchanged.
import { ApiContainer } from '../features/api/components/ApiContainer';

export function SettingsPage() {
  return <ApiContainer />;
}
