// Alerts page. Thin: composes the alerts feature container.
import { AlertsContainer } from '../features/alerts/components/AlertsContainer';

export function AlertsPage() {
  return <AlertsContainer />;
}
