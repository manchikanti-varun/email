// Lists index page. Thin: composes the lists feature container.
import { ListsContainer } from '../features/lists/components/ListsContainer';

export function ListsPage() {
  return <ListsContainer />;
}
