// Single Email Check page. Thin: composes the verification feature panel.
import { SingleVerificationPanel } from '../features/verification/components/SingleVerificationPanel';

export function SingleCheckPage() {
  return <SingleVerificationPanel />;
}
