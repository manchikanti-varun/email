// Dashboard page. Thin: composes the dashboard feature container.
import { DashboardContainer } from '../features/dashboard/components/DashboardContainer';

export function DashboardPage() {
  return <DashboardContainer />;
}
